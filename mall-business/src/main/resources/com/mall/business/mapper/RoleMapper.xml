<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mall.business.mapper.RoleMapper">
    <sql id="roleColumns">
        id,
        name,
        remark,
        data_scope,
        level,
        permission,
        create_user_id,
        create_user_name,
        create_time,
        update_user_id,
        update_user_name,
        update_time,
        is_del
    </sql>

    <select id="findRoleByUserId" resultType="com.mall.entity.RoleDO">
        SELECT
            r.permission
        FROM sys_user_role ur
        LEFT JOIN sys_role r ON ur.role_id = r.id
        <where>
            ur.user_id = #{userId}
            AND r.is_del = 0
        </where>
    </select>
</mapper>